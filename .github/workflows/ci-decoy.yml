name: decoy-ci
on:
  pull_request:
jobs:
  slow-check:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "decoy check running ($(date -u +%FT%TZ))"
          sleep 180
          echo "decoy check passed"
